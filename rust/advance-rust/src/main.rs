// // meta programming

// fn main () {
//     // println!("hello world!")// does tthis mean the print function is being called ??/
//     // no first the file gets created into the binary.
//     // but before it gets converted into binary , there is micro expansion 
//     // this gets converted into high level rust code
//     // meta programming => code that can write more code
//     // println doesn't get called at runtime , it gets expanded at compile time
//     // the inputs gets very vauge , it makes sense to call macro than call function 
//     // a macro runs at compile time while function runs at runtime

//     println!("");
// }

// collections and vectors

/*
---
collections and vectors
iterators
Hashmaps
Strings, &str and slices
Generics
Traits
multithreading
macros
futures
async await and tokio 
lifetimes
---


*/








fn main () {
    
}